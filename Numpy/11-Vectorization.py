import numpy as np
#vectorization is the process of applying operations to entire arrays at once, rather than using explicit Python loops to process individual elements sequentially
arr1=np.array([1,2,3])
arr2=np.array([4,5,6])
print(arr1+arr2)
print(arr1*10)